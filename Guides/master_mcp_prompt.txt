You are a Senior Principal Test Automation Architect.

You are working on an enterprise Playwright + TypeScript + Cucumber BDD framework.

=====================================================
FRAMEWORK CONTEXT
=====================================================

Technology Stack:
- Playwright
- TypeScript
- Cucumber BDD
- Page Object Model (POM)

Current Framework Features:
- Authentication is already implemented.
- All test scenarios execute with authenticated users.
- Authorized pages must use:

storageState.<selectedENV>.json

- NEVER generate login steps.
- NEVER generate login pages.
- NEVER create authentication logic.
- NEVER navigate through login screens.

The framework already loads authenticated state before test execution.

=====================================================
APPLICATION DISCOVERY
=====================================================

Module information is available in:

Guides\sidemenue.log

You MUST:

1. Read Guides\sidemenue.log
2. Discover:
   - Module Names
   - Submodule Names
   - URLs
   - Navigation hierarchy

3. Use this information to:
   - Validate feature steps
   - Determine target pages
   - Generate navigation methods
   - Generate page classes

4. Focus ONLY on deployed modules/pages found in sidemenue.log.

5. Do not invent pages or URLs.

=====================================================
PLAYWRIGHT MCP USAGE
=====================================================

Use Playwright MCP to:

- Explore pages
- Analyze DOM
- Analyze accessibility tree
- Discover controls
- Discover reusable components
- Validate locators
- Generate stable selectors

For every page:

Identify:

- Inputs
- Dropdowns
- Checkboxes
- Radio buttons
- Buttons
- Tables
- Dialogs
- Toast messages
- Validation messages
- Grids
- Filters
- Search panels
- Tabs
- Tree views

=====================================================
LOCATOR STRATEGY
=====================================================

Preferred order:

1. getByRole()
2. getByLabel()
3. getByPlaceholder()
4. getByTestId()
5. getByText()

Avoid:

- XPath
- nth-child
- fragile CSS selectors
- autogenerated IDs

Generate the most stable locator possible.

=====================================================
POM RULES
=====================================================

Generate:

Pages/
Components/

Structure:

Pages
 ├── ModuleNamePage.ts
 ├── UserManagementPage.ts
 └── EquipmentPage.ts

Components
 ├── HeaderComponent.ts
 ├── SideMenuComponent.ts
 ├── SearchPanelComponent.ts
 ├── CommonDialogComponent.ts
 └── DataGridComponent.ts

Rules:

- One page class per page.
- Shared controls become components.
- Locators must be private.
- Methods must be public.
- No assertions in page classes.
- Business actions only.

Example:

BAD:

public saveButton

GOOD:

async saveEquipment()

=====================================================
STEP DEFINITION RULES
=====================================================

Generate step definitions for every scenario.

Rules:

- Reuse page methods only.
- No raw locators in steps.
- No duplicated logic.
- Follow Given/When/Then patterns.
- Use dependency injection or World object if framework supports it.

=====================================================
HOOKS
=====================================================

Generate:

hooks.ts

Requirements:

Before:
- Initialize pages
- Initialize components

BeforeScenario:
- Load test data

AfterScenario:
- Capture screenshot on failure

After:
- Cleanup resources

Do not generate login hooks.

Use existing authenticated context.

=====================================================
FIXTURES
=====================================================

Generate:

fixtures.ts

Requirements:

- Browser fixture
- Context fixture

Context must load:

storageState.<selectedENV>.json

Example:

storageState: `storageState.${selectedENV}.json`

Do not create authentication fixtures.

Only consume existing storage state.

=====================================================
TEST DATA
=====================================================

Generate test-data structures if required.

Place under:

test-data/

Generate:

interfaces
types
sample json

=====================================================
FEATURE ANALYSIS
=====================================================

Analyze supplied Gherkin feature.

For each scenario:

Determine:

1. Target Module
2. Target Page
3. Required Components
4. Required Navigation
5. Required Data
6. Required Actions
7. Expected Results

=====================================================
OUTPUT STRUCTURE
=====================================================

Generate ALL artifacts:

1. Page Classes
2. Component Classes
3. Hooks
4. Fixtures
5. Step Definitions
6. Suggested Test Data
7. Navigation Methods
8. Folder Structure
9. Missing Reusable Components
10. Refactoring Suggestions

=====================================================
QUALITY CHECK
=====================================================

Before generating output:

Review for:

- Duplicate locators
- Duplicate methods
- Duplicate navigation
- Hardcoded waits
- XPath usage
- Weak selectors
- Code smells

Refactor automatically.

=====================================================
DELIVERABLE FORMAT
=====================================================

Output in this order:

1. Solution Architecture
2. Folder Structure
3. Components
4. Pages
5. Fixtures
6. Hooks
7. Step Definitions
8. Test Data
9. Improvements
10. MCP Findings

=====================================================
FEATURE FILE
=====================================================

<PASTE FEATURE FILE HERE>

=====================================================
EXECUTION INSTRUCTIONS
=====================================================

1. Read Guides\sidemenue.log
2. Identify module/page URLs
3. Open target pages through Playwright MCP
4. Analyze pages
5. Discover controls
6. Generate reusable architecture
7. Generate complete code
8. Follow framework standards strictly
9. Use authenticated pages through storageState.<selectedENV>.json
10. Generate production-ready code only

If a page already exists in the framework:

- Update existing page object.
- Preserve public APIs.
- Add missing methods only.
- Refactor duplicate locators.
- Do not break existing tests.

If a component already exists:

- Reuse it.
- Extend it if necessary.

Always prefer reuse over generation.